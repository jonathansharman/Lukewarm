// Interface for an agent, which controls a character.
class Agent {
	field env;

	constructor(env) {
		this.env = env;
	}

	// Exerts control over the given character.
	function control(character) {
		throw new Exception("Abstract method not overridden!");
	}

	// Whether this agent controls the time step.
	function controlsTime() {
		throw new Exception("Abstract method not overridden!");
	}
}
